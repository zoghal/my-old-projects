PROGRAM V1;
USES
    CRT;
VAR
  F1,F2         :FILE;
  CH            :ARRAY[1..20000] OF CHAR;
  RESULT        :INTEGER;
BEGIN
CLRSCR;
ASSIGN(F1,'V1.EXE');
ASSIGN(F2,'SS.EXE');
RESET (F1);
REWRITE(F2);
BLOCKREAD(F1,CH,20000,RESULT);
BLOCKWRITE(F2,CH,20000,RESULT);
CLOSE(F1);
CLOSE(F2);
END.

